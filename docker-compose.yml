version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: local_qdrant_db
    ports:
      - "6333:6333" # HTTP API Port
      - "6334:6334" # gRPC High-Speed Port
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: always

volumes:
  qdrant_storage:
